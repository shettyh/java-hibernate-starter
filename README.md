# Hibernate starter with JPA and H2 database

This repository is a starting point for using `Hibernate`. This project uses `Hibernate JPA` and `H2` in memory database. Database can be changed in `persistence.xml`.
